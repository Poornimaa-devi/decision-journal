# Day 52 — Components

## What I worked on
Created my first real React component (Header.jsx) and replaced Vite's 
default starter content in App.jsx. Attempted a second component 
(Footer.jsx) independently as practice.

## What I learned
- The precise technical definition of a component: a capitalized function 
  returning JSX
- Key JSX rules: single root element, className instead of class, curly 
  braces for embedding JavaScript

## Challenges
[Fill in: any JSX syntax errors hit while building Footer.jsx?]

## How I solved them
[Fill in]

## Key takeaway
A component is genuinely simple at its core — just a function with two 
rules. Complexity comes later from props and state, not from the component 
concept itself.

## Next step
Day 53 — Props: passing data INTO components, so GoalCard can display 
different goal data each time it's used, rather than hardcoded text.