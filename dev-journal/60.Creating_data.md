# Day 60 — Creating Data

## What I worked on
Updated handleAddGoal in App.jsx to send a real POST request to my Express 
backend instead of just updating local state, using the actual saved 
MongoDB document in the response.

## What I learned
- The three required pieces of a POST fetch: method, Content-Type header, 
  and JSON.stringify'd body
- Why using the server's real response (not a local guess) matters — 
  correct _id, correct schema defaults
- GoalForm required no changes at all, proving the value of the 
  function-as-prop pattern from Day 55

## Challenges
[Fill in: any 400/401 errors? Did Content-Type get forgotten initially?]

## How I solved them
[Fill in]

## Key takeaway
Full-stack create flow is now genuinely working: React form → Express API 
→ MongoDB → back to React, with real persistence confirmed via refresh.

## Next step
Day 61 — Updating Data: making GoalCard's "Mark as Completed" button 
actually PATCH the real backend, instead of only toggling local state.