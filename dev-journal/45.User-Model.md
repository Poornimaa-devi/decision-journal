# Day 45 — User Model

## What I worked on
Built models/User.js with email/password fields, a pre-save hook for 
automatic password hashing, and a comparePassword instance method.

## What I learned
- Mongoose pre-save hooks and how to automatically transform data before 
  it's written to the database
- Why isModified("password") prevents double-hashing bugs
- Why arrow functions break "this" binding inside Mongoose hooks

## Challenges
[Fill in: still unresolved MongoDB connection issue may have blocked testing 
this end-to-end today]

## How I solved them
[Fill in once connection is fixed and tested]

## Key takeaway
Password hashing now happens automatically and safely, built into the Model 
itself rather than relying on controllers to remember to call bcrypt manually.

