# Day 40 — MongoDB + Mongoose (Schemas & Models)

## What I worked on
Created my first Mongoose Schema and Model (Goal.js) inside a new models/ folder.

## What I learned
- The difference between a Schema (blueprint) and a Model (usable tool)
- Mongoose's naming convention: "Goal" model → "goals" collection automatically
- Why schema-level validation complements, rather than replaces, my existing 
  middleware validation

## Challenges
[Fill in if any — e.g. confusion about where to place the models folder]

## How I solved them
[Fill in]

## Key takeaway
My Goal data finally has a real, enforced shape at the database level — not 
just informal rules scattered across middleware and controller code.

## Next step
Day 41 — Rewire goalController.js to actually use this Model instead of the 
in-memory array — real persistent CRUD begins.