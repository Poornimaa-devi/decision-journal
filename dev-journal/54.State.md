# Day 54 — State

## What I worked on
Added useState to GoalCard.jsx to make the completed status interactive, 
toggled by a button click, with each card maintaining independent state.

## What I learned
- Why plain variable reassignment doesn't update the UI, but useState's 
  setter function does
- The critical difference between calling a function immediately (onClick={fn()}) 
  vs passing a reference (onClick={fn}) 
- Each rendered instance of a component has its own independent state

## Challenges
[Fill in: any infinite loop or "too many re-renders" errors while learning 
the onClick reference vs call distinction?]

## How I solved them
[Fill in]

## Key takeaway
Components can now genuinely respond to user interaction and update 
themselves — this is the missing piece between "static display" (props) 
and a real interactive UI.

## Next step
Day 55 — Events & Forms: handling more interaction types (text input, 
form submission) beyond simple button clicks, preparing for real goal 
creation forms.