# Day 36 — Databases (SQL vs NoSQL)

## What I worked on
Studied the theory of databases — no coding today. Focused on understanding 
why my in-memory array-based backend (goalController.js, progressController.js) 
is temporary and needs to be replaced with a real database.

## What I learned
- The core difference between SQL (tables, fixed schema) and NoSQL (documents, 
  flexible schema)
- Why MongoDB fits a project like Decision Journal, where data shape may evolve

## Challenges
None coding-wise today — mostly building the mental model before touching code.

## Key takeaway
My `let goals = [...]` array is essentially a placeholder database that resets 
every server restart. Days 37-42 will replace this with real, persistent MongoDB storage.

## Next step
Day 37 — MongoDB fundamentals (documents, collections, BSON) and getting MongoDB 
running locally or via Atlas.